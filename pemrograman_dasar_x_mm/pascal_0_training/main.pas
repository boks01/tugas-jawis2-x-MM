program calculator;
var x:integer;
var op:string;
var y:integer;
procedure addition(x, y);
 writeln('the result is',x+y);
 readln;
begin
procedure subtraction(x, y);
 writeln('the result is',x-y);
 readln;
begin
    
end;
begin
    write('num 1 : ');
    readln(x);
    write('operation (+, -) : ');
    readln(x);
    write('num 2 : ');
    readln(y);
end;